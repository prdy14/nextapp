import { PropsWithChildren } from "react";

export default function Nav(props: PropsWithChildren) {
  return (
    <div>
      <div className="absolute flex flex-col border">{}</div>
    </div>
  );
}
